#!/usr/bin/sh

echo -e 'Backing up $HOME and /etc'
echo -e '========================='

cd /
sudo rsync -aAXvPh --dry-run --delete \
--exclude=/home/*/.cache \
--exclude=/home/*/.local/share/Trash \
--exclude=/home/*/.thumbnails \
/home /etc "/mnt/Backup/$(hostname)"
